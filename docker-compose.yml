version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3007:3007"
    environment:
      NODE_ENV: production
    command: npm start
